use crate::ui::ui_helpers::*;
use crate::backend::classes::planner::Planner;

pub fn init_ui() {
    loop {
        ui_helpers.display_menu();
        match ui_helpers.get_choice() {

        }
        
    }
}